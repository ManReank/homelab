Proxy

Direktori ini menyimpan konfigurasi proxy untuk kebutuhan network lab.

Proxy dapat digunakan untuk:

HTTP/HTTPS forwarding
akses keluar dari network internal
testing reverse proxy
eksperimen routing aplikasi

Konfigurasi service tidak disimpan di sini jika service tersebut dijalankan langsung di dalam VM atau container tertentu.
